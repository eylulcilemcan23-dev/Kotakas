const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let ME=null;
async function api(path,opt={}){const init={credentials:'same-origin',...opt};if(opt.body&&typeof opt.body!=='string'){init.headers={'content-type':'application/json',...(opt.headers||{})};init.body=JSON.stringify(opt.body)}const r=await fetch(path,init);let d={};try{d=await r.json()}catch{}if(!r.ok){const e=new Error(d.error||`HTTP_${r.status}`);e.status=r.status;e.data=d;throw e}return d}
function toast(t){let e=$('#toast');if(!e){e=document.createElement('div');e.id='toast';e.className='toast';document.body.append(e)}e.textContent=t;e.style.display='block';clearTimeout(e._t);e._t=setTimeout(()=>e.style.display='none',2500)}
function fmtGB(n){return Number(n||0).toLocaleString('tr-TR',{minimumFractionDigits:2,maximumFractionDigits:2})+' GB'}
function formatDate(s){try{return new Intl.DateTimeFormat('tr-TR',{dateStyle:'short',timeStyle:'short'}).format(new Date(s))}catch{return s||''}}
async function loadMe(){try{ME=(await api('/api/me')).user}catch{ME=null}return ME}
function panelHref(){if(!ME)return'/login.html';if(String(ME.role).startsWith('admin_'))return'/admin.html';if(ME.role==='trader')return'/trader.html';return'/dashboard.html'}
function updateNav(){const auth=$$('[data-auth]'),guest=$$('[data-guest]');auth.forEach(x=>x.style.display=ME?'':'none');guest.forEach(x=>x.style.display=ME?'none':'');$$('[data-user-name]').forEach(x=>{if(ME){x.textContent=ME.displayName||ME.email;if(x.tagName==='A')x.href=panelHref()}})}
window.toggleMobile=()=>$('#mobileNav')?.classList.toggle('open');
window.logout=async()=>{await api('/api/logout',{method:'POST'}).catch(()=>{});location.href='/'};
window.authTab=name=>{const login=name==='login';$('#loginForm').style.display=login?'':'none';$('#registerForm').style.display=login?'none':'';$('#loginTab')?.classList.toggle('active',login);$('#registerTab')?.classList.toggle('active',!login)};
function authPage(){if(location.pathname.endsWith('/register.html'))authTab('register');$('#loginForm')?.addEventListener('submit',async e=>{e.preventDefault();try{const d=await api('/api/login',{method:'POST',body:Object.fromEntries(new FormData(e.target))});ME=d.user;location.href=panelHref()}catch{toast('E-posta veya şifre hatalı.')}});$('#registerForm')?.addEventListener('submit',async e=>{e.preventDefault();try{const d=await api('/api/register',{method:'POST',body:Object.fromEntries(new FormData(e.target))});ME=d.user;location.href='/dashboard.html'}catch(err){toast(err.data?.error==='email_already_registered'?'Bu e-posta zaten kayıtlı.':'Kayıt oluşturulamadı.')}})}
async function renderHome(){try{const d=await api('/api/sale-requests');const rows=d.requests||[];const set=(id,v)=>{const e=document.getElementById(id);if(e)e.textContent=v};set('statRequests',rows.length);const box=$('#homeRequests');if(box)box.innerHTML=rows.slice(0,4).map(r=>{const os=r.offers||[],best=os[0];return `<div class="listitem"><div class="itemhead"><div><div class="itemtitle">${esc(r.itemName)}</div><div class="meta">${esc(r.serverCode)} • ${r.quantity} adet • Min ${fmtGB(r.minimumGb)}</div></div><div class="spacer"></div><span class="pill ${os.length?'green':''}">${os.length} teklif</span></div>${best?`<div class="actions"><span class="pill gold">En iyi: ${esc(best.traderName)} • ${fmtGB(best.priceGb)}</span></div>`:''}</div>`}).join('')||'<div class="empty">Henüz açık satış talebi yok.</div>'}catch{}}
